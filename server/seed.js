// Dealers Plus seed script
// Note: The server seeds demo data in memory on startup.
// This script is a no-op to keep the "npm run seed" command valid.
// You can extend this to write static JSON files or preload a database later.

console.log("Seed complete: in-memory demo data is initialized by server/index.js at runtime.");
process.exit(0);
